<%= form_for(@stock) do |f| %>

<div class = "form-group">
  <% if @stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stock.errors.count, "error") %> prohibited this stock from being saved:</h2>
 </div>
      <ul>
      <% @stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class = "field">
   <font color="#ffffff"> <%= f.label :ticker %><br> </font>
   <font color= "#333333"> <%= f.text_field :ticker %></font>
  </div>
  
  <div class = "field">
  <font color="#ffffff">  <%= f.label :quantity %><br> </font>
  <font color= "#333333">  <%= f.text_field :quantity %> </font>
  </div>

  <div class = "field">
    <%= f.text_field :user_id, value: current_user.id, type: :hidden %>
  </div>
  
  <div class="field">
  <font color= "#333333">  <%= f.submit %> </font>
  </div>
 
<% end %>
